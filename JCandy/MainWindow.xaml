<Window x:Class="JCandy.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:JCandy"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="8*" />
            <ColumnDefinition Width="2*" />
        </Grid.ColumnDefinitions>

        <TextBlock Text="Java Archive (.jar) :" Margin="15,15,0,0" />
        <TextBox Name="jarBox" Grid.Row="1" Margin="15"/>
        <Button Grid.Row="1" Grid.Column="1" Content="Browse" Margin="0,15,15,15" />

        <TextBlock Text="Icon (.ico) :" Grid.Row="2" Margin="15,0,0,0" />
        <TextBox Name="iconBox" Grid.Row="3" Margin="15"/>
        <Button Grid.Row="3" Grid.Column="1" Content="Browse" Margin="0,15,15,15" />
    </Grid>
</Window>
